gets a mob level con.
### Arguments
**Name**|**Type**|**Description**
:---|:---|:---
iOtherLevel||

### Example

```perl
my $iOtherLevel = 1;
my $val = $mob->GetLevelCon($iOtherLevel);
quest::say($val); # Returns uint
```